{
  "private": true,
  "scripts": {
    "build": "tstl",
    "dev": "tstl --watch",
    "main": "tstl && cts run ./build/main.lua"
  },
  "devDependencies": {
    "typescript-to-lua": "^1.34.0"
  }
}
